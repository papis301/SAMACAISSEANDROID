<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        tools:ignore="CoarseFineLocation" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SamaCaisseAndroid"
        tools:targetApi="31">
        <activity
            android:name=".ui.ClientManagementActivity"
            android:exported="false"
            android:label="@string/title_activity_client_management"
            android:theme="@style/Theme.SamaCaisseAndroid" />
        <activity android:name=".ui.ProductManagementActivity" />
        <activity
            android:name=".AdminAuthScreen"
            android:exported="true"
            android:label="@string/title_activity_admin_auth_screen"
            android:theme="@style/Theme.SamaCaisseAndroid">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UserManagementActivity"
            android:exported="false" />
        <activity
            android:name=".AdminLoginActivity"
            android:exported="true"></activity>
        <activity
            android:name=".ConnecteImprimantee"
            android:exported="true"></activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"></activity>
    </application>

</manifest>